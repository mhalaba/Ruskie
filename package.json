{
  "name": "ru-garrison-atlas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Weryfikowalny atlas garnizonów Sił Zbrojnych FR — źródła otwarte, szczebel brygada/dywizja, bez teatru ukraińskiego.",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-data.mjs && node scripts/validate-data.mjs && vite build",
    "validate": "node scripts/validate-data.mjs",
    "build-data": "node scripts/build-data.mjs",
    "geocode": "node scripts/geocode.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
